# Verification Checklist: memory-search-get-decoupling

> **Scenarios:** scenarios.md | **Spec:** specs/memory-search-decoupled/spec.md

## S — Scenario verification

| # | 场景 | 来源 | 验证方式 | 验证命令/步骤 | 期望结果 | 状态 |
|---|------|------|----------|--------------|----------|------|
| S1 | search 召回按类型多样性返回 9 条 | scenarios.md:L? | unit-test | `cd extensions/personal-assistant && ../../node_modules/.bin/vitest --run test/search.test.ts` — `it("per-type cap returns 9 results max")` | 4 rule + 4 fact + 4 process → 9 results, indices [0,3,6]=rule, [1,4,7]=fact, [2,5,8]=process | [ ] |
| S2 | get endpoint 用于 webui 预览,不 bump | scenarios.md:L? | unit-test | `cd packages/webui/server && ../../../../node_modules/.bin/vitest --run test/memory-routes.test.ts` — `it("does not bump access_count on GET preview")` | after GET, `getAtom(id).access_count === 0`, `last_access === null` | [ ] |
| S3 | agent memory_get tool 拿到完整 atom | scenarios.md:L? | unit-test | `cd extensions/personal-assistant && ../../node_modules/.bin/vitest --run test/memory-tool.test.ts` — `it("returns full content + bumps access_count for valid id")` | execute returns content+details; `access_count` becomes 1; `last_access` is set | [ ] |
| S4 | extraction 时强语气词驱动 high importance | scenarios.md:L? | unit-test | `cd extensions/personal-assistant && ../../node_modules/.bin/vitest --run test/extraction-prompt.test.ts` — `it("STRONG tone signals produce 0.85 hint")` | "千万记得每次 commit 前跑 check" → `scoreUserTone().level === "strong"`, `importanceHint === 0.85` | [ ] |
| S5 | formatMemoryContext 注入的 memory block 含 id | scenarios.md:L? | unit-test | `cd extensions/personal-assistant && ../../node_modules/.bin/vitest --run test/format.test.ts` — `it("includes id line, no file line")` | block contains `id: <uuid>`, does NOT contain `file:` | [ ] |
| S6 | search recall injection 把 search 结果喂给 LLM | scenarios.md:L? | unit-test | `cd extensions/personal-assistant && ../../node_modules/.bin/vitest --run test/format.test.ts` — `it("formatMemoryContext sorts by distance asc, not score desc")` | result with low score / high cosine appears before result with high score / low cosine | [ ] |
| S7 | get 不存在的 atom id 返回 404 | scenarios.md:L? | unit-test | `cd packages/webui/server && ../../../../node_modules/.bin/vitest --run test/memory-routes.test.ts` — `it("returns 404 if atom not found")` | HTTP 404, body.error contains "not found" | [ ] |
| S8 | memory_get 工具收到不存在的 id | scenarios.md:L? | unit-test | `cd extensions/personal-assistant && ../../node_modules/.bin/vitest --run test/memory-tool.test.ts` — `it("returns not_found for unknown id")` | text contains "atom not found: missing"; details.error === "not_found"; no row mutated | [ ] |
| S9 | search 命中 0 atom | scenarios.md:L? | unit-test | `cd extensions/personal-assistant && ../../node_modules/.bin/vitest --run test/search.test.ts` — `it("returns empty array when no atoms match")` | `recallAtoms` returns `[]` | [ ] |
| S10 | search 命中 atom 但 .md 文件丢失 | scenarios.md:L? | unit-test | `cd extensions/personal-assistant && ../../node_modules/.bin/vitest --run test/search.test.ts` — `it("returns atom even when .md file is missing")` | result returned, no error (search is discovery only) | [ ] |
| S11 | ollama 不可用,embedText 返回 null | scenarios.md:L? | unit-test | `cd extensions/personal-assistant && ../../node_modules/.bin/vitest --run test/search.test.ts` — `it("returns empty array when ollama is unreachable")` | `recallAtoms` returns `[]`, no fallback path | [ ] |
| S12 | search topK 超过 9 | scenarios.md:L? | unit-test | `cd extensions/personal-assistant && ../../node_modules/.bin/vitest --run test/search.test.ts` — `it("caps at 9 even when topK=100")` | 9 results returned regardless of topK | [ ] |
| S13 | extraction LLM 返回 invalid JSON | scenarios.md:L? | unit-test | `cd extensions/personal-assistant && ../../node_modules/.bin/vitest --run test/extraction.test.ts` — `it("returns null on invalid JSON")` | `parseExtractionJson("not json")` returns null | [ ] |
| S14 | webui MemoryEditor 点击 atom 详情预览(不 bump) | scenarios.md:L? | unit-test | `cd packages/webui/server && ../../../../node_modules/.bin/vitest --run test/memory-routes.test.ts` — `it("does not bump access_count on GET preview")` | after GET, access_count === 0 | [ ] |
| S15 | per-type top-3 时 type 稀疏 | scenarios.md:L? | unit-test | `cd extensions/personal-assistant && ../../node_modules/.bin/vitest --run test/search.test.ts` — `it("round-robin skips sparse type slots")` | 1 rule + 0 fact + 2 process → [rule@0, process@0, process@1] | [ ] |
| S16 | per-type top-3 时 type 完全空 | scenarios.md:L? | unit-test | `cd extensions/personal-assistant && ../../node_modules/.bin/vitest --run test/search.test.ts` — `it("returns 0 results when all types empty")` | `recallAtoms` returns `[]` | [ ] |
| S17 | tone scoring 完全中性消息 | scenarios.md:L? | unit-test | `cd extensions/personal-assistant && ../../node_modules/.bin/vitest --run test/extraction-prompt.test.ts` — `it("NEUTRAL messages return level=neutral, no hint injected")` | `scoreUserTone().level === "neutral"`; prompt contains no `<user_tone>` segment | [ ] |
| S18 | tone scoring 混合强弱语气 | scenarios.md:L? | unit-test | `cd extensions/personal-assistant && ../../node_modules/.bin/vitest --run test/extraction-prompt.test.ts` — `it("STRONG wins over WEAK in mixed-tone messages")` | max-tier-wins rule: STRONG > HABIT > WEAK > RARE | [ ] |
| S19 | tone scoring 跨多句 | scenarios.md:L? | unit-test | `cd extensions/personal-assistant && ../../node_modules/.bin/vitest --run test/extraction-prompt.test.ts` — `it("scans across all messages")` | tone detection aggregates over all messages, not just first | [ ] |
| S20 | get atom content hash mismatch | scenarios.md:L? | unit-test | `cd packages/webui/server && ../../../../node_modules/.bin/vitest --run test/memory-routes.test.ts` — `it("returns content='' if .md file hash mismatch (stale)")` | content === "" | [ ] |
| S21 | search topK=0 | scenarios.md:L? | unit-test | `cd extensions/personal-assistant && ../../node_modules/.bin/vitest --run test/search.test.ts` — `it("returns empty array when topK=0")` | `recallAtoms(...{topK:0})` returns `[]` | [ ] |
| S22 | per-type top-3 with concurrent write | scenarios.md:L? | unit-test | `cd extensions/personal-assistant && ../../node_modules/.bin/vitest --run test/search.test.ts` — `it("does not crash when getAtom returns null mid-search")` | candidate with missing atom row is skipped silently | [ ] |
| S23 | per-type top-3 排序按 round-robin 交错 | scenarios.md:L? | unit-test | `cd extensions/personal-assistant && ../../node_modules/.bin/vitest --run test/search.test.ts` — `it("round-robin interleaves type slots")` | types at indices [0,3,6] / [1,4,7] / [2,5,8] | [ ] |
| S24 | per-type 全空 | scenarios.md:L? | unit-test | `cd extensions/personal-assistant && ../../node_modules/.bin/vitest --run test/search.test.ts` — `it("returns empty when all 3 types have zero matching atoms")` | `recallAtoms` returns `[]` | [ ] |
| S25 | per-type 内部 score DESC 排序(加权公式) | scenarios.md:L? | unit-test | `cd extensions/personal-assistant && ../../node_modules/.bin/vitest --run test/search.test.ts` — `it("sorts by score DESC within type")` | 3-atom fixture scores 1.05/0.8925/0.876 → order [1.05, 0.8925, 0.876] | [ ] |
| S26 | score 边界值 — cosine=0 → score=0 | scenarios.md:L? | unit-test | `cd extensions/personal-assistant && ../../node_modules/.bin/vitest --run test/search.test.ts` — `it("cosine=0 yields score=0 regardless of strength/importance")` | score === 0 even with strength=1, importance=1 | [ ] |
| S27 | score 边界值 — cosine=1 → score=1.5 | scenarios.md:L? | unit-test | `cd extensions/personal-assistant && ../../node_modules/.bin/vitest --run test/search.test.ts` — `it("cosine=1 with max boost yields score=1.5")` | score === 1.5 | [ ] |
| S28 | cosine 主导性验证 | scenarios.md:L? | unit-test | `cd extensions/personal-assistant && ../../node_modules/.bin/vitest --run test/search.test.ts` — `it("cosine dominates boost within max-boost range")` | atom with cosine=0.85+0/0 boost beats cosine=0.6+1.0/1.0 boost only when cosine gap is small | [ ] |
| S29 | rule type strength = importance 时的简化 | scenarios.md:L? | unit-test | `cd extensions/personal-assistant && ../../node_modules/.bin/vitest --run test/search.test.ts` — `it("rule atoms: strength tracks importance")` | 5 rule atoms with importance 0.5..0.9 (strength=importance), cosine=0.8 → order by score DESC mirrors importance DESC | [ ] |
| S30 | fact type strength 衰减后 importance 不变 | scenarios.md:L? | unit-test | `cd extensions/personal-assistant && ../../node_modules/.bin/vitest --run test/search.test.ts` — `it("decayed strength reduces score even when importance is constant")` | A(strength=0.7)/B(strength=0.1) same cosine+importance → A.score > B.score | [ ] |
| S31 | strength feedback 循环 | scenarios.md:L? | unit-test | `cd extensions/personal-assistant && ../../node_modules/.bin/vitest --run test/memory-tool.test.ts` — `it("search does not bump, memory_get does bump")` | 3-step sequence: search (count=0) → memory_get (count=1) → search (count=1, no further bump) | [ ] |
| S32 | cosine 接近时 boost 起决定作用 | scenarios.md:L? | unit-test | `cd extensions/personal-assistant && ../../node_modules/.bin/vitest --run test/search.test.ts` — `it("near-tied cosine: boost decides")` | atoms with cosine 0.70 / 0.71 / 0.69 → order determined by strength/importance | [ ] |
| S33 | formatMemoryContext 注入时再做 distance asc 全局排序 | scenarios.md:L? | unit-test | `cd extensions/personal-assistant && ../../node_modules/.bin/vitest --run test/format.test.ts` — `it("formatMemoryContext uses distance asc sort, not score desc")` | (S6 covers this; verification duplication noted) | [ ] |
| S34 | tone scoring 边界词 "如果" | scenarios.md:L? | unit-test | `cd extensions/personal-assistant && ../../node_modules/.bin/vitest --run test/extraction-prompt.test.ts` — `it("'如果' returns WEAK")` | "如果今天有空..." → level="weak", importanceHint=0.35 | [ ] |
| S35 | runDecay 与 score 公式的协同 (遗忘 → 清理 完整链条) | scenarios.md:L? | unit-test | `cd extensions/personal-assistant && ../../node_modules/.bin/vitest --run test/decay.test.ts` — `it("fact with 30 days no-access: strength drops below archiveThreshold")` | runDecay archives the fact; runDecay sequence also verified by existing decay tests | [ ] |
| S36 | rule type 永不 archive(即使 strength 极低) | scenarios.md:L? | unit-test | `cd extensions/personal-assistant && ../../node_modules/.bin/vitest --run test/decay.test.ts` — `it("rule type is never archived by runDecay")` | existing decay test confirms rule atoms survive runDecay | [ ] |

## R — Requirement verification

| # | 需求 | 来源 | 验证方式 | 期望证据 | 状态 |
|---|------|------|----------|----------|------|
| R1 | memory_get tool | spec.md ADDED #1 | code-review + unit-test | `pi.registerTool({ name: "memory_get", ... })` in extensions/personal-assistant/memory.ts:5.1; `MemoryGetParams = Type.Object({ id: Type.String(...) })`; test/memory-tool.test.ts passes | [ ] |
| R2 | user_tone hint in extraction prompt | spec.md ADDED #2 | unit-test | `scoreUserTone` exported from extraction.ts; `buildExtractionPrompt` injects `<user_tone>` + `<importance_hint>`; tests cover STRONG/HABIT/WEAK/RARE/NEUTRAL + EXTRACT_PROMPT_V2 paragraph | [ ] |
| R3 | weighted score formula for search ranking | spec.md ADDED #3 | unit-test | `score = cosine × (1 + 0.3 × strength + 0.2 × importance)` in search.ts; tests cover boundary (cosine=0/1), dominance, within-type sort | [ ] |
| R4 | per-type top-3 recall | spec.md ADDED #4 | unit-test | 3 parallel `vectorSearch(embedding, 3, { type: "rule" | "fact" | "process" })` calls; round-robin interleave; sparse-type skip; tests cover full/sparse/empty scenarios | [ ] |
| R5 | search does not mutate access state | spec.md ADDED #5 | unit-test | No `index.updateAccess` call in search.ts; test asserts `access_count` stays 0 after search; only `memory_get` tool bumps | [ ] |
| R6 | formatMemoryBlock emits id, not file_path | spec.md ADDED #6 | unit-test | format.ts block contains `id: <uuid>` and not `file:`; format.test.ts assertions updated | [ ] |
| R7 | RecallResult shape | spec.md MODIFIED #1 | code-review + unit-test | types.ts RecallResult has `score: number`, no `file_path`; types.test.ts asserts both | [ ] |
| R8 | webui GET /api/memory/:id is preview-only | spec.md MODIFIED #2 | code-review + unit-test | memory.ts registerGetMemoryById has no updateAccess call; regression test confirms `access_count === 0` after GET | [ ] |
| R9 | webui search response shape | spec.md MODIFIED #3 | unit-test | registerPostSearch response includes `score`, no `file_path`; memory-routes.test.ts updated | [ ] |
| R10 | (REMOVED) search-bumps-access-count | spec.md REMOVED #1 | code-review | No call sites of `updateAccess` in search.ts; grep confirms only memory_get tool calls it | [ ] |
| R11 | (REMOVED) search-returns-file-path | spec.md REMOVED #2 | code-review | grep `r.file_path` in search.ts returns nothing; format.ts block has no `file:` line | [ ] |
| R12 | (REMOVED) file-in-format-block | spec.md REMOVED #3 | code-review | grep `file:` in format.ts returns nothing | [ ] |

## Notes

- All 36 scenarios map to existing or planned test cases. No scenarios lack coverage.
- All 9 ADDED + 3 MODIFIED requirements map to test cases or code-review evidence.
- REMOVED requirements (3) only need code-review (no positive test needed).
- Source file:lines above are approximate at write-time; actual line numbers will shift during implementation. Verify against the final tree.