name: pi
description: A Dart agent framework for building AI-powered applications with tool execution, streaming events, and session persistence.
version: 0.1.0
repository: https://github.com/arrrrny/pi-mono
topics:
  - ai
  - agent
  - llm
  - chatbot

environment:
  sdk: ^3.0.0

dependencies:
  http: ^1.4.0
  yaml: ^3.1.3

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
