# Changelog

## [Unreleased]

### Removed

- Satellite `list`/`find`/`grep` sub-tools removed. Use `bash` for directory listing, file search, and content search. Client and server must be upgraded together.
