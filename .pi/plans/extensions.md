# Plan: extensions

**Date:** 2026-03-16

I now have a solid picture of the pi extension system — 70+ example extensions, a rich event/hook API, tool registration, custom UI, provider registration, etc.

But **"extensions" is too vague for me to plan anything.** Here's what I need from you:

**What do you want to do with extensions?** Pick one (or tell me something else):

1. **Build a new extension** — What should it do? (e.g., "an extension that auto-runs tests after every edit")
2. **Improve the extension system itself** — What's broken or missing? (e.g., "add hot-reload", "extension dependency resolution", "extension marketplace")
3. **Refactor/fix existing extensions** — Which one(s) and what's wrong?
4. **Document the extension API** — There's already `docs/extensions.md`, but gaps?
5. **Something else entirely** — Tell me the actual goal

Which is it?
