# 为 pi 贡献

这份指南的目的是节省双方时间。

[English](CONTRIBUTING.md) | 简体中文

## 一条规则

**你必须理解自己的代码。** 如果你无法解释你的变更做了什么，以及它们如何与系统其他部分交互，你的 PR 会被关闭。

使用 AI 写代码没有问题。但提交自己并不理解的 AI 生成低质内容不行。

如果你使用 agent，请从 `pi-mono` 根目录运行它，这样它会自动读取 `AGENTS.md`。你的 agent 必须遵守该文件中的规则和指南。

## 贡献门槛

新贡献者提交的所有 issue 和 PR 默认都会被自动关闭。

周五到周日提交的 issue 不会被审查。如果事情紧急，请在 Discord 询问：https://discord.com/invite/3cU7Bz4UPx

维护者每天会审查自动关闭的 issue，并重新打开有价值的内容。不符合下方质量标准的 issue 不会被重新打开，也不会收到回复。

批准通过维护者在 issue 中的回复完成：

- `lgtmi`：你之后提交的 issue 不会被自动关闭
- `lgtm`：你之后提交的 issue 和 PR 不会被自动关闭

`lgtmi` 不授予提交 PR 的权限。只有 `lgtm` 才授予提交 PR 的权限。

## Issue 质量标准

如果你创建 issue，必须使用两个 GitHub issue 模板之一。

如果你创建 issue，请保持简短、具体，并值得阅读。

- 保持简洁。如果一屏放不下，就太长了。
- 用你自己的表达来写。
- 清楚说明 bug 或请求。
- 解释为什么它重要。
- 如果你想自己实现这个变更，请明确说明。

如果 issue 真实且写得好，维护者可能会重新打开它，回复 `lgtmi`，或回复 `lgtm`。

## 封禁

如果你两次忽略本文档，或者用 agent 生成的 issue 刷 tracker，你的 GitHub 账号会被永久封禁。

如果你通过自动化发送大量 issue，你的 GitHub 账号会被永久封禁。没有反悔机会。

## 提交 PR 前

除非你已经通过 `lgtm` 获得批准，否则不要打开 PR。

提交 PR 前运行：

```bash
npm run check
./test.sh
```

两者都必须通过。

不要编辑 `CHANGELOG.md`。Changelog 条目由维护者添加。

如果你要向 `packages/ai` 添加新的 provider，请查看 `AGENTS.md` 中要求的测试。

## 设计理念

pi 的核心保持极简。如果你的功能不属于核心，它应该作为扩展存在。让核心膨胀的 PR 很可能会被拒绝。

## 有问题？

请在 [Discord](https://discord.com/invite/nKXTsAcmbT) 提问。

## FAQ

### 为什么新 issue 和 PR 会被自动关闭？

pi 收到的 issue 数量超过维护者能够实时负责审查的范围。许多报告不符合本指南的质量标准，或者没有遵守 CONTRIBUTING.md。有些内容是不经提交者审阅和整理、被 agent 机械地抛到仓库里的。自动关闭提供了缓冲，让维护者能按自己的节奏审查 tracker，并重新打开符合质量标准的 issue。

### 为什么周末提交的 issue 不会被审查？

维护者需要不被 issue tracker 打断的休息时间。周五到周日提交的 issue 会被自动关闭，且不进入周一审查队列。如果问题紧急，请在 Discord 询问，并附上简短说明、复现步骤和相关日志。

### 为什么有些 issue 没有回复？

回复本身也是维护工作。低信号 issue、不清楚的报告、重复内容，以及不遵守本指南的 issue 可能会被直接关闭而不讨论。这能把时间留给可复现的 bug、有思考的请求，以及真正把报告整理到可执行状态的贡献者。

### 为什么不让 AI 处理所有 triage？

AI 可以帮助归并重复项、总结报告并发现缺失信息。但它不被信任去做最终维护者决策。润色过的 AI 生成 issue 仍可能是错误的、误导性的，或调查成本很高。最终门槛仍由人类审查。

### 这对贡献者有敌意吗？

不是。这是防止维护者倦怠和 tracker 垃圾内容的护栏。简短、具体、可复现的 issue 是受欢迎的。有思考的贡献是受欢迎的。自动化生成的低质内容、理所当然的索取，以及大量低投入报告不受欢迎。
