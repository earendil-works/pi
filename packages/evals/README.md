# pi-evals

Rust evaluation harness for running real `AgentSession` prompts and collecting native messages, output, usage, cost, and elapsed time. Comparative helpers calculate deterministic baseline/candidate pass-rate lift.
