{
	"name": "@tculpepp/pi",
	"version": "0.66.1",
	"description": "CLI tool for managing vLLM deployments on GPU pods",
	"type": "module",
	"bin": {
		"pi-pods": "dist/cli.js"
	},
	"scripts": {
		"clean": "shx rm -rf dist",
		"build": "tsgo -p tsconfig.build.json && shx chmod +x dist/cli.js && shx cp src/models.json dist/ && shx cp -r scripts dist/",
		"prepublishOnly": "npm run clean && npm run build"
	},
	"files": [
		"dist",
		"scripts"
	],
	"keywords": [
		"llm",
		"vllm",
		"gpu",
		"ai",
		"cli"
	],
	"author": "Tom Culpepper",
	"contributors": [
		{ "name": "Mario Zechner", "url": "https://github.com/badlogic" }
	],
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/tculpepp/secure-pi-mono.git",
		"directory": "packages/pods"
	},
	"engines": {
		"node": ">=20.0.0"
	},
	"dependencies": {
		"@tculpepp/pi-agent-core": "^0.66.1",
		"chalk": "^5.5.0"
	},
	"devDependencies": {}
}
