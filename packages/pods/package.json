{
	"name": "@kennyfrc/mu",
	"version": "0.24.56",
	"description": "CLI tool for managing vLLM deployments on GPU pods",
	"type": "module",
	"bin": {
		"mu-pods": "dist/cli.js"
	},
	"scripts": {
		"clean": "rm -rf dist",
		"build": "tsgo -p tsconfig.build.json && chmod +x dist/cli.js && cp src/models.json dist/ && cp -r scripts dist/",
		"check": "biome check --write .",
		"prepublishOnly": "npm run clean && npm run build"
	},
	"files": [
		"dist",
		"scripts"
	],
	"keywords": [
		"llm",
		"vllm",
		"gpu",
		"ai",
		"cli"
	],
	"author": "Mario Zechner",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/kennyfrc/mu-mono.git",
		"directory": "packages/pods"
	},
	"publishConfig": {
		"access": "restricted"
	},
	"engines": {
		"node": ">=20.0.0"
	},
	"dependencies": {
		"@kennyfrc/mu-agent-core": "^0.24.56",
		"chalk": "^5.5.0"
	},
	"devDependencies": {}
}
