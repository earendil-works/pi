# pi-session-backend-sqlite

Native Rust SQLite session repository. It uses WAL, transactional entry/branch updates, one-writer ownership checks, shared or per-session database layouts, safe physical filename encoding, read-only listing, forks, and foreign-source snapshots. No Node.js SQLite adapter is required.
