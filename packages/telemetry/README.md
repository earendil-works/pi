# pi-telemetry

Vendor-neutral Rust telemetry contracts with explicit context propagation. `InMemoryTelemetryContext` records deterministic parentage, attributes, ordered events, status, and settlement; `NOOP_TELEMETRY_CONTEXT` provides a zero-storage adapter.
