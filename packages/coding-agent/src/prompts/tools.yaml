# Full tool descriptions sent to the LLM as part of tool definitions
# These are the detailed descriptions that help the model understand how to use each tool

read: |
  Read the contents of a text file. Supports text files only. For text files, defaults to first 2000 lines. Use offset/limit for large files.

read_image: |
  Analyze an image and extract information based on an objective. Use this tool only for image files or image URLs. Supported formats: jpg, jpeg, png, gif, webp. Do not use this tool for reading text files. Accepts a local file path or remote URL. Returns XML-delimited analysis output.

  Parameters:
  - path: Path to the image file (local or URL)
  - objective: What to extract/analyze from the image
  - context (optional): Why this analysis matters (helps focus the analysis)
  - referenceFiles (optional): Paths to reference images for comparison
  - model (optional): "gemini" (default: gemini)

  Examples:
  {"path":"docs/specs/system-design.png","objective":"Summarize main architectural decisions","context":"Evaluating this system for a new project"}

  {"path":"screenshots/before.png","objective":"Identify all visual differences","context":"Reviewing UI changes","referenceFiles":["screenshots/after.png"]}

bash: |
  Execute a bash command in the current working directory. Returns stdout and stderr. Optionally provide a timeout in seconds (default: 1800 seconds / 30 minutes). Minimum timeout is 1800 seconds; lower values are ignored.

edit: |
  Edit a file using hashline mode (preferred) or legacy text matching.

  **Hashline Mode (New - Use This):**
  Read file output includes hashline format: `LINE:HASH|content`
  Use the `LINE:HASH` reference to specify edit location:

  ```json
  {
    "path": "file.ts",
    "edits": [
      { "set_line": { "anchor": "5:a3", "new_text": "const x = 1;" } },
      { "replace_lines": { "start_anchor": "1:bg", "end_anchor": "3:0e", "new_text": "// replaced" } },
      { "insert_after": { "anchor": "10:ff", "text": "// new line" } }
    ]
  }
  ```

  Operations:
  - `set_line`: Replace single line. Use empty `new_text` to delete.
  - `replace_lines`: Replace line range (inclusive). Use empty `new_text` to delete range.
  - `insert_after`: Insert after specified line.
  - `replace`: Fuzzy text fallback (use when hashline unavailable).

  **Important:**
  - Copy `LINE:HASH` refs exactly from read output
  - Multiple edits in one call are validated atomically (all-or-nothing)
  - If hash mismatch detected, error shows updated LINE:HASH refs
  - Re-read file before editing if file may have changed

  **Legacy Mode (Backward Compatible):**
  ```json
  {
    "path": "file.ts",
    "oldText": "function hello()",
    "newText": "function greet()",
    "all": false
  }
  ```

apply_patch: |
  Use the `apply_patch` tool to edit files.
  Your patch language is a stripped-down, file-oriented diff format designed to be easy to parse and safe to apply. You can think of it as a high-level envelope:

  *** Begin Patch
  [ one or more file sections ]
  *** End Patch

  Within that envelope, you get a sequence of file operations.
  You MUST include a header to specify the action you are taking.
  Each operation starts with one of three headers:

  *** Add File: <path> - create a new file. Every following line is a + line (the initial contents).
  *** Delete File: <path> - remove an existing file. Nothing follows.
  *** Update File: <path> - patch an existing file in place (optionally with a rename).

  May be immediately followed by *** Move to: <new path> if you want to rename the file.
  Then one or more "hunks", each introduced by @@ (optionally followed by a hunk header).
  Within a hunk each line starts with:

  For instructions on [context_before] and [context_after]:
  - By default, show 3 lines of code immediately above and 3 lines immediately below each change. If a change is within 3 lines of a previous change, do NOT duplicate the first change's [context_after] lines in the second change's [context_before] lines.
  - If 3 lines of context is insufficient to uniquely identify the snippet of code within the file, use the @@ operator to indicate the class or function to which the snippet belongs. For instance, we might have:
  @@ class BaseClass
  [3 lines of pre-context]
  - [old_code]
  + [new_code]
  [3 lines of post-context]

  - If a code block is repeated so many times in a class or function such that even a single @@ statement and 3 lines of context cannot uniquely identify the snippet of code, you can use multiple @@ statements to jump to the right context. For instance:

  @@ class BaseClass
  @@     def method():
  [3 lines of pre-context]
  - [old_code]
  + [new_code]
  [3 lines of post-context]

  The full grammar definition is below:
  Patch := Begin { FileOp } End
  Begin := "*** Begin Patch" NEWLINE
  End := "*** End Patch" NEWLINE
  FileOp := AddFile | DeleteFile | UpdateFile
  AddFile := "*** Add File: " path NEWLINE { "+" line NEWLINE }
  DeleteFile := "*** Delete File: " path NEWLINE
  UpdateFile := "*** Update File: " path NEWLINE [ MoveTo ] { Hunk }
  MoveTo := "*** Move to: " newPath NEWLINE
  Hunk := "@@" [ header ] NEWLINE { HunkLine } [ "*** End of File" NEWLINE ]
  HunkLine := (" " | "-" | "+") text NEWLINE

  A full patch can combine several operations:

  *** Begin Patch
  *** Add File: hello.txt
  +Hello world
  *** Update File: src/app.py
  *** Move to: src/main.py
  @@ def greet():
  -print("Hi")
  +print("Hello, world!")
  *** Delete File: obsolete.txt
  *** End Patch

  It is important to remember:

  - You must include a header with your intended action (Add/Delete/Update)
  - You must prefix new lines with `+` even when creating a new file
  - File references may be relative or absolute.
    - `~` and `~/...` are expanded to the user's home directory.
    - Relative paths are resolved from the current working directory (and may refer to paths outside it).

  You can invoke apply_patch like:

  ```
  {"input":"*** Begin Patch\n*** Add File: hello.txt\n+Hello, world!\n*** End Patch\n"}
  ```

write: |
  Write content to a file. Creates the file if it doesn't exist, overwrites if it does. Automatically creates parent directories.

grep: |
  Search file contents for a pattern. Returns matching lines with file paths and line numbers. Respects .gitignore by default. Set `includeIgnored: true` to search gitignored files.

glob: |
  Find files by glob pattern OR list directory contents. When pattern is provided, searches for matching files (e.g., '*.ts', '**/*.json'). When pattern is omitted, lists directory contents with '/' suffix for directories. Respects .gitignore by default. Set `includeIgnored: true` to find gitignored files.

list_threads: |
  List past conversation threads. Returns thread IDs, relative dates, message counts, workspace paths, and preview text. Supports keyword search (message content) and workspace filtering (case-insensitive substring match).

  **Defaults to current working directory** if workspace is not specified. Use longer unique segments to avoid false matches (e.g., "work/mu-mono" instead of "mu-mono").

  **Search syntax:**
  - Space-separated terms use AND logic: `auth login` matches threads with both "auth" AND "login"
  - Use quotes for exact phrases: `"error handling"` matches that exact phrase
  - Use `|` for OR within a group: `doc|docs sidebar` matches (doc OR docs) AND sidebar
  - Example: `auth login|signin "error handling"` matches threads with auth AND (login OR signin) AND "error handling"

read_thread: |
  Read the conversation history of a specific thread by its ID.
  
  IMPORTANT: 'goal' is REQUIRED - describe what information you need from the thread. An AI will extract only the relevant parts.
  
  Use 'raw: true' ONLY when you need the full unfiltered transcript (e.g., for complete context or debugging).
  
  Returns <thread_extract> (default) or <reference_thread> (raw mode).
  
  Tool calls (bash, read, write, etc.) are visible by default. Use 'detailed: false' for clean transcripts without tool output.

todo_write: |
  Create and manage a structured task list for multi-step work.

  States: pending, in_progress, completed, cancelled.
  Priority: high, medium (default), low.
  Replaces the full list each call.

todo: |
  Create and manage durable, file-backed todos stored on disk.

  Todos are stored as individual markdown files with YAML front-matter.
  Default directory: <repoRoot>/.mu/todos/
  Override with MU_TODO_PATH (absolute or repo-relative).

  Actions (set the 'action' field):
  - list: list todos (supports filters: list, status, tags, assignment)
  - get: get full todo (front-matter + body)
  - create: create a new todo
  - create_many: create multiple todos in one call
  - update: update fields and/or body (replaces content)
  - append: append markdown to the body
  - claim / release: assignment coordination for parallel agents
  - claim_next: atomically claim the next matching unassigned todo
  - delete: delete a todo (may require force when assigned)

  Status:
  - open | in_progress | done | cancelled

  Notes:
  - Prefer claim_next for parallel-safe automation.
  - Use list + get to load minimal context: avoid pulling all bodies.

handoff: |
  Explicitly hand off to a new session with specific file context injected.

  Use when: current context is cluttered, switching focus areas, or need fresh start with curated context.

  Parameters:
  - goal: Imperative statement of what to accomplish (required)
  - files: Array of file selections using slice syntax (required):
    - 'src/foo.ts' - full file
    - 'src/foo.ts:42' - single line
    - 'src/foo.ts:10-50' - line range
    - 'src/foo.ts:100-' - from line 100 to end
  - token_limit: Max tokens for file context (default: 100000)

  The tool validates files exist, checks token limits, and prepares the handoff.
  On success, the session switches and the goal with file context is auto-submitted.

  Example:
  {"goal":"Implement OAuth logout flow","files":["src/auth/oauth.ts:50-150","src/routes/auth.ts","src/types/auth.ts:1-30"]}

exec_command: |
  Execute a shell command.

  Parameters:
  - cmd: Shell command to execute (required)
  - workdir (optional): Working directory
  - shell (optional): Shell program (may be ignored)
  - login (optional): Run as login shell (may be ignored)
  - tty (optional): Allocate a TTY (may be ignored)
  - yield_time_ms (optional): Streaming yield interval (may be ignored)
  - max_output_tokens (optional): Max output to return (may be ignored)
  - sandbox_permissions / justification / prefix_rule: accepted for compatibility (ignored in mu)

  Example:
  {"cmd":"ls -la","workdir":"packages/coding-agent"}

view_image: |
  Load an image (local path or URL) and attach it to the tool result so the model can view it.

  Parameters:
  - path: Local path or URL to an image (jpg, jpeg, png, gif, webp)

  Example:
  {"path":"screenshots/ui.png"}

update_plan: |
  Update the current plan (durable + file-backed).

  This tool replaces the full plan each call and writes it to disk under:
  - <repoRoot>/.mu/plans/<sessionId>.json
  Override the directory with MU_PLAN_PATH.

  Parameters:
  - explanation (optional): Short explanation of the update
  - plan: Array of { step: string, status: "pending" | "in_progress" | "completed" }

  Example:
  {"explanation":"Break into smaller steps","plan":[{"step":"Add tests","status":"in_progress"},{"step":"Implement tool","status":"pending"}]}
