# Full tool descriptions sent to the LLM as part of tool definitions
# These are the detailed descriptions that help the model understand how to use each tool

read: |
  Read the contents of a file. Supports text files and images (jpg, png, gif, webp). Images are sent as attachments. For text files, defaults to first 2000 lines. Use offset/limit for large files.

bash: |
  Execute a bash command in the current working directory. Returns stdout and stderr. Optionally provide a timeout in seconds (default: 1800 seconds / 30 minutes).

edit: |
  Edit a file by replacing text. Supports whitespace-flexible matching: if exact match fails, tries unescaping (\\n→newline) and flexible whitespace. Use 'all: true' to replace multiple occurrences. Provides suggestions if text not found.

write: |
  Write content to a file. Creates the file if it doesn't exist, overwrites if it does. Automatically creates parent directories.

grep: |
  Search file contents for a pattern. Returns matching lines with file paths and line numbers. Respects .gitignore.

find: |
  Search for files by glob pattern. Returns matching file paths relative to the search directory. Respects .gitignore.

ls: |
  List directory contents. Returns entries sorted alphabetically, with '/' suffix for directories. Includes dotfiles.

list_threads: |
  List past conversation threads. Returns thread IDs, relative dates, message counts, and preview text. Supports optional keyword search and project path.

read_thread: |
  Read the full transcript of a specific thread by its ID. Returns markdown-formatted conversation with user messages, assistant responses, and tool results (truncated for large outputs). Supports optional project path.
