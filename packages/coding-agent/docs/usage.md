# Usage

```text
pi [options] [--] [@files...] [messages...]
```

Important options include `--provider`, `--model`, `--thinking`, `--tools`, `--exclude-tools`, `--session`, `--continue`, `--no-session`, `--approve`, and `--offline`. Run `pi --help` for the authoritative list.

Interactive commands include `/new`, `/name`, `/model`, `/thinking`, `/compact`, `/session`, and `/quit`. Enter submits, Shift/Alt+Enter inserts a newline, Escape aborts, and Ctrl+C twice exits.
