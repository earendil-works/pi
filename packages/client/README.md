# pi-client

Async Rust client for Pi protocol version 8. It validates the server identity handshake, correlates concurrent requests, publishes service updates and attachment changes, supports cancellation, and includes Unix-domain socket discovery.
