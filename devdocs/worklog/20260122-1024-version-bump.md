# Worklog: Version bump + build + commit + npm link

- Initialized pin/worklog.
- Committed grep fix (fix: handle leading-dash grep patterns).
- Version bump/build commits completed; restored intentional Purr notification sound (committed).
- Ran `npm link -w packages/coding-agent`.
