# Worklog: Grep leading-dash patterns

- Initialized pin and worklog.
- Updated grep tool args to use `-e` + `--`.
- Verified leading-dash patterns with tsx script.
- Added vitest cases for leading-dash patterns.
- Verified with `npm -w packages/coding-agent test` and `npm run check`.
