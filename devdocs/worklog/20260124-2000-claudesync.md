# Worklog: claudesync accounts-format update (20260124-2000-claudesync)

- Planned: Update `claudesync` in `~/.bashrc` to read/write Anthropic OAuth tokens via `.anthropic.accounts[].credentials` (active account), while maintaining legacy top-level fields.

- Updated: `~/.bashrc` `claudesync()` now:
  - Reads refresh token from `.anthropic.accounts[]` using `.anthropic.activeAccountId` (fallback to legacy `.anthropic.refresh`).
  - Writes refreshed/synced tokens back into the active account credentials, and also updates legacy top-level fields.
  - Sets `oauth.json` perms to 0600 on write.

- Verified:
  - `bash -lc 'source ~/.bashrc >/dev/null 2>&1; claudesync'` (refresh endpoint failed, keychain fallback succeeded)
  - `jq -r '.anthropic.activeAccountId' ~/.pi/agent/oauth.json` returned an ID
  - `jq` check confirmed `.anthropic.accounts[].credentials.expires` is populated for the active account
  - `stat -f %Lp ~/.pi/agent/oauth.json` returned `600`

- Follow-up (403 revoked):
  - Investigated refresh endpoint behavior.
  - Anthropic refresh requests to `https://console.anthropic.com/v1/oauth/token` returned:
    - `HTTP 400 {"error":"invalid_grant","error_description":"Refresh token not found or invalid"}`
  - Updated `claudesync` to stop claiming success when refresh/keychain refresh is invalid, and instead emit a clear re-auth instruction.
