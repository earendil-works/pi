# Worklog: queue processing + editor draft loss

- Updated: packages/coding-agent/src/tui/tui-renderer.ts
  - Removed editor clearing on user message_start events so drafts persist during queue drain.

Verification
- npm run check (pass)
