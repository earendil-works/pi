# Worklog: pi → mu rename

## What changed
- **7 package.json** files: names (`@kennyfrc/pi-*` → `@kennyfrc/mu-*`), bin entries (`pi` → `mu`, `pi-proxy` → `mu-proxy`), dependency refs, repo URLs (`pi-mono` → `mu-mono`)
- **Root package.json**: name `pi-monorepo` → `mu-monorepo`, all workspace refs in scripts
- **tsconfig.json**: path aliases `@kennyfrc/pi-*` → `@kennyfrc/mu-*`
- **.gitignore**: `.pi_config/` → `.mu_config/`
- **File rename**: `pi-codex-bridge.{ts,d.ts,js,*.map}` → `mu-codex-bridge.*` (5 files)
- **~65 .ts source files**: import paths, config paths (`.pi` → `.mu`), env vars (`PI_*` → `MU_*`), branding (logo, help text, notifications, user-agent), function names (`buildCodexPiBridge` → `buildCodexMuBridge`), constants (`PI_STATIC_INSTRUCTIONS` → `MU_STATIC_INSTRUCTIONS`), originator (`"pi"` → `"mu"`), temp prefixes, test fixtures
- **2 shell scripts**: `PI_API_KEY` → `MU_API_KEY`, CLI command refs
- **~24 in-tree compiled artifacts** (.d.ts, .js, .map in packages/ai/src/)
- **11 doc files** (READMEs, CHANGELOG, theme.md, oauth docs): all pi → mu refs
- **2 YAML/JSON files**: prompts/tools.yaml, theme-schema.json
- **~/.pi/agent/AGENTS.md + 2 docs**: `~/.pi/agent/` → `~/.mu/agent/` path refs
- **pi-mono.code-workspace** → **mu-mono.code-workspace**
- **PLAN.md**: `@kennyfrc/pi-ai` → `@kennyfrc/mu-ai`

## What we ran
- `npm install` → success (regenerated package-lock.json, new symlinks)
- `npm run build` → success (all 6 packages)
- `npm run check` → success (biome + tsgo --noEmit)
- `npm run test` → 72 tests pass, 0 failures
- Comprehensive rg sweeps for remaining pi references → clean

## Assumptions / defaults
- No migration/fallback code for `~/.pi` → `~/.mu` (user does `mv ~/.pi ~/.mu` manually)
- npm scope stays `@kennyfrc`
- OpenAI originator changed from `"pi"` to `"mu"` (arbitrary client ID, safe)
- Historical session data left untouched
- Repo URLs updated to `mu-mono` (user renames GitHub repo separately)
