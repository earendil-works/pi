# Worklog: steer queue mode + queueMessageByAgentEnd/queueMessageNext

- Created pin: `devdocs/pins/20260205-2201-steer-queue.md`

## Slice 1
- Added Agent queue API:
  - `queueMessageByAgentEnd()` (renamed semantics)
  - `queueMessageNext()` (priority enqueue, FIFO)
  - kept `queueMessage()` as deprecated alias
- Added priority queue accounting via `priorityQueueLength`.
- Updated tests + added new tests for priority ordering and `queueMode: "steer"`.

Verification:
- `npm test -w @kennyfrc/mu-agent-core` (pass)

## Slice 2
- Added new queue mode `steer` to coding-agent settings and `/queue` selector.
- Updated TUI queuing behavior:
  - normal modes enqueue via `Agent.queueMessageByAgentEnd()`
  - steer mode enqueues via `Agent.queueMessageNext()` and maintains UI mirror ordering via `queuedSteerCount`
- Added coding-agent unit test for settings persistence of `steer`.

Verification:
- `npm run check -w @kennyfrc/mu-coding-agent` (pass)
- `npm test -w @kennyfrc/mu-coding-agent` (pass)

## Slice 3
- Updated `packages/coding-agent/README.md` to document `steer` queue mode.

Verification:
- `npm run check` (pass; Biome applied small formatting fixes)
