# Worklog: Glob tool investigation (20260122-2102)

- Read: `packages/coding-agent/src/tools/glob.ts`, `packages/coding-agent/src/tools-manager.ts`, `.gitignore`, `packages/coding-agent/src/prompts/tools.yaml`.
- Bash: `ls -la devdocs`, `git status --porcelain`, `fd` glob experiments.
- Ran `globTool.execute` via `npx tsx`:
  - pattern `devdocs/**` path `.` -> no matches.
  - pattern `pins/**` path `devdocs` -> no matches.
  - pattern `*` path `devdocs` -> matches entries.
- Verified `devdocs/reports/` directory does not exist.
- Updated `packages/coding-agent/src/tools/glob.ts` with prefix-splitting and prefix-aware formatting.
- Added `should handle path-prefixed patterns` test in `packages/coding-agent/test/tools.test.ts`.
- Ran: `npm run test -w @kennyfrc/pi-coding-agent -- test/tools.test.ts -t "path-prefixed"` (pass).
- Ran: `npm run check` (pass).
