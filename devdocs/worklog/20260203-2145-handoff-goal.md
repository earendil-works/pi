# Worklog: Require explicit handoff goal

- Read README.md and package READMEs to satisfy project requirements.
- Searched handoff/autohandoff code paths (`rg -n "autohandoff|handoff"`).
- Inspected `handoff.ts`, `tui-renderer.ts`, `prompts/index.ts`, `auto-handoff.ts`, and tool descriptions.
- Ran an exploratory script with `npx tsx` to confirm the auto-handoff goal fallback and handoff message tail:
  - `getAutoHandoffGoalPrompt()` includes "Continue the current task".
  - `buildHandoffMessage()` outputs `## Goal\nContinue the current task` with the generic message.
- No source changes made.

- Implemented goal normalization:
  - Added `packages/coding-agent/src/handoff-goal.ts`.
  - Auto-handoff now normalizes LLM-generated goals; if empty/generic, derives from last user message.
  - Handoff tool now normalizes generic/empty goals using the first selected file name.
- Updated `getAutoHandoffGoalPrompt()` to remove the "Continue the current task" fallback instruction.
- Added tests: `packages/coding-agent/test/handoff-goal.test.ts`.
- Added integration coverage asserting `handoffTool.execute` normalizes generic goals.
- Updated prompt test expectations.
- Verified:
  - `npm test -w @kennyfrc/mu-coding-agent` (pass)
  - `npm run check` (pass)
