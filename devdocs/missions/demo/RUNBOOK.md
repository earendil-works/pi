# Runbook

1. Read `SPEC.md`, `TASKS.json`, `PROGRESS.md`, and this file before making changes.
2. Work exactly one task at a time.
3. Keep edits scoped to `devdocs/missions/demo/`.
4. After each meaningful change, update `TASKS.json` and `PROGRESS.md` so they stay accurate.
5. If a task cannot be completed, mark it `blocked` with the exact reason.
