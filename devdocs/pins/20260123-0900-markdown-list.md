# Pin: Fix ordered list numbering in TUI markdown

## Goal
Fix markdown renderer so ordered lists honor the list start number (no repeated "1.") in tui/coding-agent.

## Constraints
- No type `any` unless unavoidable.
- Must run `npm run check` after code changes.
- Keep changes minimal and focused.

## Current State
- Markdown renderer now honors `token.start` for ordered list bullets and uses list/table type guards to avoid Generic tokens.
- `npm run check` passes.

## Next Step
None.

## Verification
- `npm run check`
