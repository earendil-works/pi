# Pin: Require explicit handoff goal

Goal
- Ensure handoff/autohandoff always carries a specific, non-generic goal (avoid default "Continue the current task").

Constraints
- No source edits yet (context discovery only so far).
- Run `npm run check` after code changes.
- Follow project rules (no `npm run dev`, no `any`).

Current state
- `buildHandoffMessage` always appends `## Goal` with whatever `goal` string is passed.
- Added goal normalization so auto-handoff never emits the generic placeholder goal.
- Handoff tool now normalizes generic/empty goals using file names as a fallback.
- Auto-handoff goal prompt no longer instructs models to output "Continue the current task".
- Unit tests cover goal normalization behavior.

Next step
- None (implemented and verified).

Verification
- `npm test -w @kennyfrc/mu-coding-agent`
- `npm run check`
