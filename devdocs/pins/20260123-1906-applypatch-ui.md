# Pin: ApplyPatch UI diff + deletion confirmation (2026-01-23)

## Goal
Render ApplyPatch output with diff-colored preview and confirm deletions afterward in the TUI.

## Constraints
- No new `any` types.
- ApplyPatch tool returns stdout; extend details with parsed metadata.
- Keep non-interactive flows non-blocking.

## Current state
- ApplyPatch parser + tests added, tool now returns parsed metadata.
- ToolExecutionComponent renders ApplyPatch diff preview with colors.
- Deletion confirmation message removed from TUI.
- Added UI test for ApplyPatch rendering.

## Next step
- Await feedback or adjust formatting/confirmation wording if desired.

## Verification
- npx vitest --run packages/coding-agent/src/tools/apply-patch/parse.test.ts
- npx vitest --run packages/coding-agent/src/tui/apply-patch-ui.test.ts
- npm run check
