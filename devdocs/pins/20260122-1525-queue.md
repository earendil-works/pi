# Pin: queue processing + editor draft loss

Goal
- Understand how queued messages are processed and why the editor draft disappears when a queued message is sent.

Constraints
- Context discovery only; no source edits yet.

Current state
- Agent queueing lives in packages/agent/src/agent.ts (messageQueue, queueMode, drainQueueAfterPrompt).
- UI queueing lives in packages/coding-agent/src/tui/tui-renderer.ts (queuedMessages, editingQueueIndex, savedEditorText).
- Removed unconditional editor.setText("") in message_start(user) so drafts aren't cleared on queue drain.

Next step
- Verify with npm run check.
