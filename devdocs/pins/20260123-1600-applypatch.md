# Pin: apply_patch tool research (2026-01-23)

## Goal
Confirm whether openai/codex includes an apply_patch tool and assess whether it makes sense to port into this repo.

## Constraints
- Use gh-viewer to inspect openai/codex.
- Read-only on source files; no repo code changes.

## Current state
- gh-viewer search shows apply_patch tool instructions and handlers in codex (apply_patch tool instructions + tool handler/runtime + apply-patch crate).
- Local coding-agent tool list (packages/coding-agent/src/tools/index.ts) does not include apply_patch.

## Next step
Summarize findings (tool presence + structure) and provide recommendation about porting based on current tooling.

## Verification
- gh-viewer reads + local tool-list inspection via npx tsx.
