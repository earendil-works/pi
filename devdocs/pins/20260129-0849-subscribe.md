Goal: Add /subscribe <sessionId> to mu-coding-agent to watch another session JSONL, and when a subscribed session completes a turn (assistant stopReason != toolUse), inject a synthetic tool call + tool result into the current session and prompt the LLM, without adding anything to LLM context at subscription time.

Constraints:
- Subscription is a user-facing slash command only.
- Use session JSONL as event source; minimal new abstractions.
- Do not show a pending tool call before the tool result (atomic UI update).
- Avoid new type any usage.
- Run npm run check after changes.

Current state:
- Added JSONL follower utilities (chunk parsing + turn-complete detection) in session-jsonl-follower.ts.
- Added session-jsonl-follower.test.ts; tests passing.
- Added subscription-messages.ts and subscription-messages.test.ts; tests passing.
- Added subscribe-command.ts and subscribe-command.test.ts; tests passing.
- Integrated /subscribe parsing + subscription watcher/injection flow in tui-renderer.ts.

Slice: 4 / Iteration 4 of 4
Next step: Review git status and report results; ready for user review.
