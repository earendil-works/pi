# Pin: 20260129-0837-handoff-paths

## Goal
Fix manual handoff failures caused by repo-relative paths resolving from a subdirectory.

## Spec/Context Brief
- Done means: handoff file selection prompt clarifies path base (repo root/cwd) and handoff tool resolves repo-relative paths when cwd differs.
- Constraints: XML-only file selection; avoid `any`; keep changes tight; run targeted tests per slice and `npm run check` after code changes.
- Unknowns: confirm best path resolution fallback without breaking cwd-relative paths.

## Plan Brief
- Add repo-root detection helper (walk up for .git).
- Update handoff file-selection prompt to include repo root + cwd and explicit path requirements; adjust prompt tests.
- Update handoff tool to resolve relative paths against repo root when cwd lookup fails; add test coverage.
- Verify with targeted tests per slice, then `npm run check`.

## Current State
- Added repo-root helper and updated handoff file-selection prompt with repo root/cwd path rules.
- Added repo-root fallback in handoff tool path resolution with test coverage.
- Slice 2 verification passed; npm run check passed.

## Next Step
- Handoff to next agent for final response.

## Verification
- Slice 1: npm test --workspace @kennyfrc/mu-coding-agent -- auto-handoff-prompt.test.ts
- Slice 2: npm test --workspace @kennyfrc/mu-coding-agent -- handoff.test.ts
- Final: npm run check
