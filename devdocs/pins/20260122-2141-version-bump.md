# Pin: Version bump/build/commit/link (20260122-2141)

## Goal
Bump version, build, run checks, commit all changes (including build artifacts), and run `npm link` in packages/coding-agent.

## Constraints
- Use version bump script (lockstep).
- Commit message: `chore: bump version to X.Y.Z`.
- Include build artifacts.

## Current State
- Repo has existing modified files and untracked devdocs; user approved committing all.

## Next Step
Run version bump (assume patch), then build + check, then commit, then npm link.

## Verification
- `npm run build`
- `npm run check`
