# Pin: Version bump + build + commit + npm link

Goal
- Commit the grep fix, then bump versions (patch), build, commit the bump, and npm link packages/coding-agent.

Constraints
- Use npm version scripts (no manual edits).
- Run npm run build after bump.
- Use Conventional Commits.
- Run git status before each commit; stage explicit paths only.

Current state
- Purr notification sound restored and committed; npm link completed.

Next step
- None.
