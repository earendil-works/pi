# Pin: Grep leading-dash patterns

Goal
- Update Grep tool to pass patterns safely when they start with "-" (e.g., "--categories"), and add tests.

Constraints
- Keep existing grep behavior (literal/regex/glob/context/limit/ignore rules) unchanged.
- Use `-e` for pattern and `--` before path.
- Must run `npm run check` after code changes.

Current state
- Grep tool and tests updated; verification complete.

Next step
- None.
