# Pin: Model usage cache for /model selector

Goal: Speed up /model selector opening by caching model usage stats instead of rescanning all session JSONL files every time.

Constraints:
- Do not change sorting semantics (recency vs frequency).
- No unrelated refactors.
- Avoid any `any` types.

Current state:
- loadModelUsageStats() now uses a disk-backed cache keyed by session directory + file metadata and writes .model-usage-cache.json on refresh.
- Verification script confirms cache file exists and stats load.

Next step:
- None.
