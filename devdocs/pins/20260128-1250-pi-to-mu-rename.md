# Pin: pi → mu rename

## Goal
Rename the entire project from "pi" to "mu" — package names, CLI commands, env vars, config paths, branding, docs.

## Constraints
- No migration/fallback code for ~/.pi → ~/.mu (user does `mv ~/.pi ~/.mu`)
- npm scope stays @kennyfrc
- Historical data (sessions, worklogs) left untouched
- In-tree compiled artifacts updated alongside source
- OpenAI originator "pi" → "mu" (arbitrary client ID, safe to change)

## Current State
- Slice 1 / Iteration 1 of 12: starting
- Task list: devdocs/backlog.json

## Next Step
Implement Slice 1: package.json + tsconfig.json + .gitignore
Verify: `rg '@kennyfrc/pi|pi-monorepo|pi-proxy|pi_config' package.json packages/*/package.json tsconfig.json .gitignore` → 0 matches
