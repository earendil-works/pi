# Pin: Glob tool investigation (20260122-2102)

## Goal
Fix Glob tool to support path-prefixed glob patterns (e.g., `devdocs/reports/**`).

## Constraints
- Keep changes minimal and targeted.
- Verify with focused tests and repo-wide `npm run check`.

## Current State
- Added `splitLeadingLiteralPath` and prefix-aware formatting to `packages/coding-agent/src/tools/glob.ts`.
- Added unit test for path-prefixed patterns in `packages/coding-agent/test/tools.test.ts`.
- Verified targeted test: `npm run test -w @kennyfrc/pi-coding-agent -- test/tools.test.ts -t "path-prefixed"`.
- Verified repo checks: `npm run check`.

## Next Step
None.

## Verification
1) `npm run test -w @kennyfrc/pi-coding-agent -- test/tools.test.ts -t "path-prefixed"`
2) `npm run check`
