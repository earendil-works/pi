# Pin: Version bump/build/commit/link (0.22.23)

Goal: Bump version to 0.22.23, build, run checks, commit changes, and run `npm link` in packages/coding-agent.

Constraints:
- Use npm version bump script (lockstep).
- Commit message: `chore: bump version to 0.22.23`.
- Include build artifacts from build.

Current state:
- devdocs pin/worklog files modified (not to be committed in version bump).
- Root version currently 0.22.22.

Next step:
- None.

Verification:
- `node -e "console.log(require('./package.json').version)"` should print 0.22.23.
- `npm run check`.
- `git log -1 --oneline` shows commit.
- `cd packages/coding-agent && npm link`.
