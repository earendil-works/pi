# Pin: handoff diff release

## Goal
Finalize handoff diff feature release: verify tests, bump version, build, commit, and npm link packages/coding-agent.

## Constraints
- Use `git diff` (unstaged) for selected files only.
- Diff is full-file (ignore slice ranges) and should be included in handoff message.
- Count diff text toward token_limit.
- Keep changes minimal; no unrelated edits.
- Do not touch other agents' modified files.
- Use `npm run version:patch` for version bumping.
- Run `npm run build` after version bump and commit generated artifacts separately if changed.

## Current State
- Handoff diff changes committed.
- Version bump to 0.23.2, build, and check completed; release artifacts committed.
- `npm link` run for packages/coding-agent.

## Next Step
- None.

## Verification
- Run: `cd packages/coding-agent && npx vitest test/handoff.test.ts` (pass).
- Run: `cd /Users/kennyfrc/Documents/code/work/pi-mono && npm run version:patch` (done).
- Run: `cd /Users/kennyfrc/Documents/code/work/pi-mono && npm run build` (done).
- Run: `cd /Users/kennyfrc/Documents/code/work/pi-mono && npm run check` (done).
- Run: `cd /Users/kennyfrc/Documents/code/work/pi-mono/packages/coding-agent && npm link` (done).
