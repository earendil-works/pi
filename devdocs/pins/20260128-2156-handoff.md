# Pin: 20260128-2156-handoff

## Goal
Remove tool-calling from handoff file selection; require XML-only responses parsed by the state-machine parser.

## Constraints
- Use LLM-driven file selection output passed into handoff file-context builder (no manual summary).
- Auto-submit like the Handoff tool.
- Parent thread reference should use the auto-handoff style reminder.
- Avoid type "any".
- Run targeted tests per slice and `npm run check` after code changes.

## Current State
- XML-only parser implemented; tool-calling removed from prompt and selection flow.

## Next Step
- None (implementation complete).

## Verification
- npm test --workspace @kennyfrc/mu-coding-agent -- handoff-file-selection.test.ts auto-handoff-prompt.test.ts
- npm run check
