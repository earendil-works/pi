# Pin: apply-patch TS port

## Goal
Port the Rust apply_patch tool to TypeScript, replace the binary runner, and keep golden master output identical.

## Constraints
- Match Rust apply_patch stdout/stderr behavior.
- No `any` types.
- Keep changes tight; no unrelated refactors.
- Must run `npx vitest --run packages/coding-agent/src/tools/apply-patch.golden.test.ts` and `npm run check` after changes.

## Spec/Context Brief
- Current tool shells out to vendored Rust binary via `runApplyPatchBinary`.
- Golden master tests compare characterization output to fixture in `packages/coding-agent/src/tools/apply-patch/__fixtures__/apply-patch.golden.txt`.
- Need native TS parser + apply engine that mirrors Rust behavior.

## Plan Brief
- Implement TS equivalents of Rust parser + apply logic (parse hunks, seek sequence, compute replacements, apply to filesystem, summary output).
- Add a TS runner (or replace existing) to execute the engine and return stdout/stderr/exit code.
- Switch apply-patch tool and characterization to use TS runner.
- Verify golden master test, then run `npm run check`.

## Current State
- TS apply_patch engine implemented and runner now uses it.
- Removed vendored Rust crate `tools/codex-apply-patch` and gitignore entry.
- Golden master test passes with TS implementation.
- `npm run check` completed earlier (biome auto-fixed 1 file).

## Next Step
- Ready for review.

## Verification
- `npx vitest --run packages/coding-agent/src/tools/apply-patch.golden.test.ts` (passed)
- `npm run check` (passed; auto-fixed 1 file)
