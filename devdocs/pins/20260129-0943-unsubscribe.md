Goal: Add /unsubscribe command to remove active subscriptions in mu-coding-agent, and update subscription follow-up prompt to mention ReadThread for context.

Constraints:
- Keep subscription handling minimal and consistent with existing /subscribe flow.
- Avoid introducing new uses of type any.
- Run npm run check after code changes.

Current state:
- Added /unsubscribe parsing + handler; autocomplete/onSubmit updated.
- Updated subscription injection prompt to mention ReadThread context.
- Tests and npm run check completed.

Slice: 2 / 2
Next step: Summarize changes and verification for the user.
